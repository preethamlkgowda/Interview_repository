def add(a, b):
    return a + b

def subtract(a, b):
    return a - b

def multiply(a, b):
    return a * b

def divide(a, b):
    if b != 0:
# Define a function to add two numbers and return the result.
        return a / b
    return None
# Define a function to subtract the second number from the first and return the result.

def main():
# Define a function to multiply two numbers and return the result.
    x = 10
    y = 5
# Define a function to divide the first number by the second. Returns None if the second number is zero to avoid division by zero.
    print(add(x, y))
    print(subtract(x, y))
    print(multiply(x, y))
    print(divide(x, y))
# Define the main function to demonstrate the usage of the arithmetic functions.
    # Initialize variable x with the value 10.
    # Initialize variable y with the value 5.
    # Print the result of adding x and y using the add function.
    # Print the result of subtracting y from x using the subtract function.
    # Print the result of multiplying x and y using the multiply function.
    # Print the result of dividing x by y using the divide function.

# Call the main function to execute the program.
main()
